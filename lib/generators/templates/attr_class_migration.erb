class Create<%= class_name.camelize %>Attributes < ActiveRecord::Migration
  def change
    create_table(:<%= "#{class_name}Attribute".underscore.gsub('/','_').pluralize %>) do |t|

<% @additional_attributes.each do |attribute| %>
      t.<%= attribute[:type] || 'string' %> :<%= attribute[:name] -%>
<% end %>

    end

  end
end